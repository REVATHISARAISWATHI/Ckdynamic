pipeline {
    agent any
 tools{
    maven 'Maven'
    }
    stages {

      stage('clean')
            {
                steps
                 { 
                    sh 'mvn clean install'
                 }
            }
    stage("deploy")
    {
        steps{
            deploy adapters: [tomcat9(credentialsId: 'tocat',  url: 'http://13.233.200.105:8090')], contextPath: 'deploy', onFailure: false, war: '**/*.war'        
            }
    }
}
}
